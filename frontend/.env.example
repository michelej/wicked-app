# Public API URL for the browser. Leave empty to use the Vite proxy in dev
# or the current hostname on port 8000 in production. Use /api if you deploy
# the frontend and backend behind the same origin reverse proxy.
VITE_API_URL=
